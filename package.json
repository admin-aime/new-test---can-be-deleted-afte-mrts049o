{
  "name": "crm-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:crm": "npm --prefix apps/crm run dev",
    "build:crm": "npm --prefix apps/crm run build",
    "dev:admin": "npm --prefix apps/admin run dev",
    "build:admin": "npm --prefix apps/admin run build",
    "dev": "npx concurrently \"npm run dev:crm\" \"npm run dev:admin\"",
    "build": "npm run build:crm && npm run build:admin"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
